{
  "fixture_version": "admission.v1",
  "purpose": "Deterministic PostgreSQL admission sequence for one simulation execution slot and ten waiting slots.",
  "initial_state": {"running": 1, "waiting": 9, "preflight_waiting": 4},
  "operations": [
    {"name": "tenth_waiting_simulation", "request": "simulation-create-reference", "expected": {"http_status": 202, "status": "QUEUED", "queue_position": 10, "simulation_rows_delta": 1, "worker_job_rows_delta": 1}},
    {"name": "eleventh_waiting_simulation", "request": "simulation-create-reference", "expected": {"http_status": 409, "error_code": "QUEUE_FULL", "simulation_rows_delta": 0, "worker_job_rows_delta": 0}},
    {"name": "preflight_capacity_is_independent", "request": "dataset-upload", "expected": {"http_status": 409, "error_code": "PREFLIGHT_QUEUE_FULL", "simulation_rows_delta": 0, "worker_job_rows_delta": 0}}
  ],
  "invariants": [
    "A rejected simulation creates no run, snapshot, idempotency row, or worker job.",
    "A DATASET_PREFLIGHT job never changes the simulation waiting count.",
    "The next worker claim is ordered by global enqueue_seq, not by job_type."
  ]
}
